// if it's easier to not have commas and just use spaces
// that is ok, just change the instructions in this file

ld r2
str r3
lsl r2, r3
lsr r4, r5
or  r6, r7
xor r1
rxr r2
add r3, #0
mov #10
je #1
jne #2
spc #3, #1
lut r4, #1

// if it's easier to do something like 
// ctc #0
// instead of 
// ctc a
// that is also ok

ctc a
cti b
cts c
cbf
sbs r5
dbs r6
cpy r7